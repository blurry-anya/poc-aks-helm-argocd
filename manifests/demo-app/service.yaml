apiVersion: v1
kind: Service
metadata:
  name: echo-server
  namespace: demo-app
spec:
  selector:
    app: echo-server
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
