apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace: ArgoCD applies destination.namespace (monitoring / monitoring-stage)
# Internal ingress removed; only deployment + service. External ingress is in overlays.
resources:
  - deployment.yaml
  - service.yaml
