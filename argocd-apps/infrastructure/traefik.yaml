apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
spec:
  project: infrastructure
  source:
    repoURL: https://traefik.github.io/charts
    chart: traefik
    targetRevision: "39.0.0"
    helm:
      releaseName: traefik
      values: |
        # Run alongside ingress-nginx: distinct ingress class, NodePort for local
        ingressClass:
          enabled: true
          isDefaultClass: false
          name: traefik
        # Cluster-wide RBAC so Traefik can read Ingress/Service/Endpoints in monitoring-stage (fixes 503)
        rbac:
          namespaced: false
        providers:
          kubernetesIngress:
            enabled: true
            ingressClass: traefik
            # Empty = watch all namespaces (required for Ingress in monitoring-stage)
            namespaces: []
          kubernetesCRD:
            enabled: true
            ingressClass: traefik
            namespaces: []
          kubernetesGateway:
            enabled: false
        # Gateway stays enabled (port 8000); we use Kubernetes Ingress with class "traefik"
        # so only Ingresses with ingressClassName: traefik go here (nginx handles the rest)
        service:
          type: NodePort
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
