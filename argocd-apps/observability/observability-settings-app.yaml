apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-settings-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: observability
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  source:
    repoURL: 'https://github.com/sixthnode/poc-aks-helm-argocd.git'
    path: kustomizations/observability/overlays/prod
    targetRevision: HEAD
  syncPolicy:
    automated:
      # Prune off: demo-app also deploys to monitoring; pruning would remove its resources
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

